import { Text, SafeAreaView } from "react-native";
import React from "react";

export default function PlayersNames() {
  return (
    <SafeAreaView>
      <Text>players_names</Text>
    </SafeAreaView>
  );
}
